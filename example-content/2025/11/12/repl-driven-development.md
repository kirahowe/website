;;;
{:type :post
 :title "REPL-driven development, honestly"
 :slug "repl-driven"
 :tags [:clojure :workflow]}
;;;

The slug of this entry is `repl-driven` — set explicitly in frontmatter,
overriding the filename. Everything else about its address comes from where
the file sits in the date tree.

## The point

The REPL isn't a debugging tool you reach for when things break. It's where
the program lives while you grow it. Files are just where code sleeps.
